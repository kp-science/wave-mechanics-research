---
description: สรุปสถานะโปรเจกต์ทั้งหมดแบบย่อ
argument-hint: [physics|astro|web|all]
---
# /status

## งาน
รายงานสถานะโดยใช้ **git + ls เท่านั้น** ไม่อ่านเนื้อหา:
- `git log --oneline -10` commits ล่าสุด
- `git status` ไฟล์ค้าง
- `ls lessons/physics3/waves/` + `ls lessons/physics3/SHM/` จำนวนแผน
- `ls lessons/astronomy/ep01/p*.html | wc -l` + ep02
- URL GitHub Pages ล่าสุด (ถ้ารู้)

## Token budget
- ≤100 คำรายงาน · ไม่อ่านไฟล์เนื้อหาใด ๆ
